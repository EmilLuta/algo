#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    scanf("%d", &t);
    while (t > 0) {
        t--;
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; i++) {
            printf("1 ");
        }
        printf("\n");
    }
}
