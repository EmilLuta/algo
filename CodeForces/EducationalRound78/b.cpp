#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    scanf("%d", &t);
    while (t > 0) {
        t--;
        int a, b;
        scanf("%d%d", &a, &b);
        if (a == b) {
            printf("0\n");
            continue;
        }
    }
}
