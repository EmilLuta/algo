#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n;
    scanf("%d", &n);
    vector<string> t(n * 2);
    for (int i = 0; i < n * 2; i++) {
        cin >> t[i];
    }
    string s;
    cin >> s;
}

int main() {
    int t;
    scanf("%d", &t);
    while (t > 0) {
        t--;
        solve();
    }
}
