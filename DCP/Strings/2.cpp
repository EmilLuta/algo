#include <bits/stdc++.h>

using namespace std;

//[3, 7, 5, 6, 9]

int main() {
    vector<int> a = {3, 7, 5, 6, 9};
    int min_seen = INT_MIN, max_seen = INT_MAX;
    for (int i = 0; i < n; i++) {
        max_seen = max(max_seen, a[i]);
        if (a[i] < max_seen) {

        }
    }
}
