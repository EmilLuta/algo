#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<int> a = {1, 4, 2, 1, 1, 1};
    vector<int> b = {1, 4, 2, 2};
    if (a < b) {
        printf("a mai mic\n");
    } else {
        printf("b mai mic\n");
    }
}
