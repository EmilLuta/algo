/*
ID: virgil.1
TASK: transform
LANG: C++
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
    ifstream fin("palsquare.in");
    ofstream fout("palsquare.out");
    int b;
    fin >> b;
    int number = 0;
    for (int i = 1; i <= 300; i++) {
        number += 1;
        int digit = number % b;
        int mult = 1;
        while (digit == 0) {
            number -= 
            number += 1 << (p + 1);
            digit = number % (b * (10 << (p + 1)));
            digit /= 1 << (p + 1)
        }
        if (number % b == 0) {
            
        }
    }
}
