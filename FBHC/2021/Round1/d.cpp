#include <bits/stdc++.h>

using namespace std;

void solve(int t) {
    int n, m, a, b;
    scanf("%d%d%d%d", &n, &m, &a, &b);
}

int main() {
    int t;
    scanf("%d", &t);
    for (int i = 1; i <= t; i++) {
        solve(i);
    }
}
